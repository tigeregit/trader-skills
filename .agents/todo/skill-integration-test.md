# TODO: P4 集成实测与阈值校准

来源：docs/design.md 落地路线 P4

## 背景

前三阶段产物（网关 + asgk 库 + references + 路由层）拼装完成后，需端到端验证：在真实并发场景下 skill 是否可用、网关阈值是否合理、缓存 TTL 是否得当。

## 待办

用 `docs/test-method.md` 确立的测试流程，跑典型业务流程：

- [ ] 流程 A：单票完整估值（行情 + 研报 + 信号 + 资金面 + 估值公式）。
- [ ] 流程 B：批量估值对比（并发多票，压测网关）。
- [ ] 流程 C：主题研报批量检索（iwencai + 东财 PDF）。
- [ ] 流程 D：新标的快速调研（11 步全链路）。

校准项：

- [ ] 网关各域名组阈值（东财/同花顺/财联社分别在多少并发下不封 IP）。
- [ ] 缓存 TTL（静态/半静态/实时三档的实际命中率与新鲜度平衡）。
- [ ] 降级链路：主源被封时备用源切换是否生效（`failover.md`）。

## 验收标准

- 上述 4 个流程在并发下均能完成且不触发 IP 封禁。
- 产出一份阈值/TTL 推荐值表，回填到 `sgw_config.toml`。
- 发现的问题回流为新的 todo 或对应模块的修复。

## 依赖

- 依赖 `skill-router.md`（含其前置 P0-P2）全部完成。
- 依赖 `pi-agent-test-method.md` 提供测试流程。
