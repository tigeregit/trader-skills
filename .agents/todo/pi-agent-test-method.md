# TODO: 确立 pi agent 测试方法

来源：AGENTS.md「测试方法 [TODO]」

## 背景

本项目 skill 部署在单 IP 下供 100～1000 个 agent 并发使用，普通的单 agent 手动验证不足以暴露并发流量、缓存命中、网关限流等问题，需要一套可重复的自动化测试方法。

初步意向是用 **pi agent** 做测试，但其具体用法尚未确认。

## 待办

- [ ] 查看 pi agent 官网，了解其能力与接入方式。
- [ ] 评估 pi agent 是否能模拟多 agent 并发调用 skill（并发度、是否支持同 IP 多实例）。
- [ ] 若 pi agent 适用：确定测试流程（如何发起、如何采集结果、如何观测网关计数/封 IP 情况）。
- [ ] 若不适用：调研替代方案（如 locust 自压测、多进程脚本），给出选型理由。

## 验收标准

- 在此文件补充一节「测试流程」，写明：用什么工具、怎么跑、观测哪些指标。
- 该流程能用于校准网关阈值（见 `gateway-mvp.md`）与缓存 TTL。

## 依赖

- 无强依赖；但产出的测试流程将服务于 `gateway-mvp.md`、`skill-integration-test.md`。
